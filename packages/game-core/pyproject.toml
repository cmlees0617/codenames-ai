[project]
name = "game-core"
version = "0.1.0"
description = "Codenames game types, views, and player/algorithm protocols"
requires-python = ">=3.12"

[build-system]
requires = ["uv_build>=0.11.16,<0.12"]
build-backend = "uv_build"

[tool.uv.build-backend]
module-name = "game_core"
